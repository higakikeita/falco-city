{
  "name": "falco-city",
  "private": true,
  "version": "1.0.0",
  "description": "Falco / Sysdig runtime security pipeline as a walkable 3D city",
  "scripts": {
    "build": "node build.mjs",
    "test": "node scripts/regress.mjs",
    "check": "node scripts/check-imports.mjs && node scripts/regress.mjs && node build.mjs && node scripts/check-build.mjs",
    "dev": "python3 -m http.server 8722 --directory src"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "three": "0.160.0"
  }
}